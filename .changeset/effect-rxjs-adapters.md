---
'@home-automation/rxx': minor
---

Add adapters for converting between Effect Streams and RxJS Observables. Use `streamToObservable` to convert an Effect Stream to an Observable, and `observableToStream` to convert an Observable to a Stream.
