{
  "root": "packages/rxx",
  "sourceRoot": "packages/rxx/src",
  "projectType": "library",
  "targets": {
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/rxx/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/packages/rxx"],
      "options": {
        "jestConfig": "packages/rxx/jest.config.js",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
